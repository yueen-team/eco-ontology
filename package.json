{
  "name": "@yueen-team/eco-ontology",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "packageManager": "pnpm@10.34.3",
  "description": "Versioned environmental ontology contracts for EcoCheck, eco-execution-graph, and eco-semantic-knowledge-base.",
  "scripts": {
    "prepare": "git config core.hooksPath .husky",
    "check": "node verify/verify.mjs",
    "projections:generate": "node scripts/generate-projections.mjs",
    "projections:check": "node scripts/generate-projections.mjs --check",
    "projections:fixtures": "node scripts/verify-projection-fixtures.mjs",
    "grounding:fixtures": "node scripts/verify-grounding-fixtures.mjs",
    "ledger:verify": "node scripts/verify-governance-ledger.mjs",
    "ledger:append": "node scripts/append-governance-ledger.mjs",
    "release:manifest:update": "node scripts/update-release-manifest.mjs",
    "release:manifest:check": "node scripts/update-release-manifest.mjs --check",
    "release:bundle:update": "node scripts/build-release-bundle.mjs",
    "release:bundle:check": "node scripts/build-release-bundle.mjs --check",
    "adoption:receipts": "node scripts/generate-adoption-receipts.mjs",
    "projection:provenance": "node scripts/write-projection-provenance.mjs",
    "validate:report-only": "node scripts/report-only-validate.mjs",
    "validate:blocking": "node scripts/report-only-validate.mjs --blocking",
    "verify:all": "pnpm projections:check && pnpm projections:fixtures && pnpm grounding:fixtures && pnpm ledger:verify && pnpm release:manifest:check && pnpm release:bundle:check && pnpm check && pnpm adoption:receipts && pnpm validate:report-only && pnpm validate:blocking && pnpm projection:provenance && pnpm format:check",
    "bdd:export": "node scripts/export-bdd.mjs",
    "format:check": "prettier --check .",
    "format": "prettier --write .",
    "main:ship": "node scripts/git-workflow/main-ship.mjs"
  },
  "devDependencies": {
    "@commitlint/cli": "^19.8.1",
    "@commitlint/config-conventional": "^19.8.1",
    "ajv": "^6.12.6",
    "husky": "^9.1.7",
    "lint-staged": "^16.1.2",
    "prettier": "^3.6.2"
  }
}
